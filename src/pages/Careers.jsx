import { useState } from 'react'
import PageHero from './PageHero'
import { careers } from '../data/careers'

export default function Careers() { const [selected, setSelected] = useState(null); return <main><PageHero label="CAREERS / 04" title={<>BRING YOUR<br /><em>BEST.</em></>} copy="Fictional example roles created for this academic portfolio experience." visual="hawk" /><section className="section-pad careers"><div className="career-intro"><p className="eyebrow">BUILD WHAT’S NEXT</p><h2>THE WORK IS<br /><em>THE REWARD.</em></h2><p>We value people who care deeply about their craft and generously about their collaborators.</p></div><div className="job-list">{careers.map((job, index) => <article key={job.role}><button type="button" onClick={() => setSelected(selected === index ? null : index)} aria-expanded={selected === index}><span>0{index + 1}</span><div><p className="eyebrow">{job.department}</p><h3>{job.role}</h3></div><p>{job.location}<br />{job.type}</p><i>{selected === index ? '−' : '+'}</i></button>{selected === index && <div className="job-details"><p>{job.description}</p><ul>{job.requirements.map(r => <li key={r}>{r}</li>)}</ul><a className="button button-red" href="mailto:portfolio@example.com?subject=Fictional%20role%20interest">APPLY TO THIS EXAMPLE <i>↗</i></a></div>}</article>)}</div></section></main> }
